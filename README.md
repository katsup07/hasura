# React Apollo Client Example

## Setup

1. Install dependencies:
   ```sh
   npm install
   ```
2. Start the app:
   ```sh
   npm start
   ```
3. Run codegen (after server is running):
   ```sh
   npm run codegen
   ```
